git 
-current branch: git symbolic-ref HEAD
-undo a commit: git reset --soft HEAD^
-remove a file from stage: 
-git reset HEAD -- <directoryName>
-git checkout master
-git merge branchname

-laravel new eapi
or 
-composer create-project --prefer-dist laravel/laravel AwesomeProject
-composer create-project laravel/laravel=5.7 your-project-name --prefer-dist

+Note Model relationship
+Remove a migration in 2steps
-delete database/migrations/my_migration_file_name.php
-php artisan make:migration create_users_table 
-php artisan migrate:refresh

+faker
-create faker
-set seeder 
-php artisan db:seed

+Model relationships
-here model Product is set to have many review
-review is set to belong to a Product
+Tinker
php artisan tinker
note: tinker helps you run cmd on your app good for checking functionalities

Find Justin Hileman

+Resource
-php artisan make:resource 
eg: serve test
-Transormer
Note: transformer is the way we use resource to create a standard naming for our resource (json output) for consistency. This way even if a dev changes the name of a schema attribute, that new name will not affect the return resource.

-remove suffix collection from doc header ?
-Laravel controller handles pagination 
















